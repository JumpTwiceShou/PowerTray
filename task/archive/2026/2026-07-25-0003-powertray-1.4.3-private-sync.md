# PowerTray 1.4.3 Private Development Sync And Scrollbar Merge

## Scope

- Treat the current tooltip lifecycle safe subset and red low-battery device icon as PowerTray 1.4.3 development code.
- Align tracked product/build/installer version sources to `1.4.3`.
- Commit the complete intended change set on `main` and push it only to private remote `sync`.
- Merge private branch `feat/modernize-tray-device-scrollbar` into `main` after reviewing its commit and diff scope.
- Fast-forward the canonical PowerTray checkout on Windows VM102 (`windowsvm`) and Ubuntu VM101 (`ubuntu-server`) to the pushed private commit.

## Exclusions

- No push to public `origin`.
- No public tag, GitHub Release, release asset, installer publication, or installed-runtime replacement.
- No force push, destructive cleanup, or modification of unrelated machine work.
- Do not synchronize a remote machine if its PowerTray checkout is dirty or cannot fast-forward safely.

## Ordered Work

- [x] Confirm local scope, GitHub authentication, remotes, remote delta, and canonical SSH aliases.
- [x] Fast-forward local `main` over the two documentation-only private commits while preserving the intended worktree.
- [x] Align 1.4.3 version sources and regenerate project-reference lock metadata.
- [x] Run focused validation and review the complete commit scope.
- [x] Commit and push `main` to private `sync`.
- [x] Fast-forward and verify Windows VM102.
- [x] Fast-forward and verify Ubuntu VM101.
- [x] Review and merge `feat/modernize-tray-device-scrollbar` into private `main`.
- [x] Push the merged `main` and synchronize the current Windows machine, Windows VM102, and Ubuntu VM101.
- [x] Update local design memory and archive this task.

## Acceptance Criteria

- Private `sync/main`, local `main`, Windows VM102 `main`, and Ubuntu VM101 `main` resolve to the same new commit.
- All synchronized checkouts have clean tracked worktrees.
- Version-bearing product/build/installer files report `1.4.3`; historical 1.4.2 documentation remains unchanged.
- Tooltip safe-subset and red battery alert tests/builds remain valid.
- Public `origin/main` and formal installed 1.4.2 are untouched.

## Evidence

- Initial local branch: `main` at `adbf76819c64ca780a5d26258336ab522d617eaa`, two documentation-only commits behind `sync/main`.
- Intended worktree consists of `LGSTrayUI/BatteryIconDrawing.cs`, `LGSTrayUI/LogiDeviceIcon.xaml.cs`, `PowerTray.Tests/Program.cs`, and the two archived task records.
- `gh 2.96.0` is authenticated as `JumpTwiceShou`; private `sync` is `git@github.com:JumpTwiceShou/PowerTray-dev.git`.
- Canonical aliases resolve to Windows VM102 through `windowsvm` and Ubuntu VM101 through `ubuntu-server`.
- Saved the full worktree in `pre-1.4.3-private-sync-20260725-0003`, fast-forwarded `main` from `adbf768` to `7ae818d`, restored the worktree without conflicts, and dropped only that completed temporary stash.
- Updated UI, HID helper, installer default, and installer-build default to `1.4.3`; forced restore updated the `PowerTrayHID` project reference in `PowerTray.Tests/packages.lock.json` to `[1.4.3, )`.
- SDK 8.0.423 MSBuild completed locked Debug and Release solution rebuilds. Release UI/helper metadata reports `FileVersion 1.4.3.0` and `ProductVersion 1.4.3+7ae818d...` before the final commit.
- The test program passed the tooltip formatting and new red battery icon rendering tests, then reached its existing directional IPC test at `Program.cs:619`; that final IPC test could not create its fixed pipe while formal 1.4.2 is running locally.
- Committed the intended 1.4.3 code and prior archived task records as `ae8a56f36c1149171c6bd06ad9d1be7a7d73fc27` (`Prepare PowerTray 1.4.3 tooltip and alert fixes`) and pushed it to private `sync/main`; `git ls-remote` confirmed the same commit.
- Ubuntu VM101 was clean on `main`; `git fetch origin main` followed by `git merge --ff-only origin/main` advanced it from `7ae818d` to `ae8a56f`, and its worktree remained clean.
- Windows VM102's canonical worktree was on `feat/modernize-tray-device-scrollbar` at `a4852dc` with a pre-existing untracked `NUL`. To preserve that development state, no checkout, cleanup, stash, or worktree rewrite was performed. Because local `main` was not checked out and was a fast-forward, `git fetch origin main:main` safely advanced only its `main` reference from `7ae818d` to `ae8a56f`; the current feature branch, HEAD, and status remained byte-for-byte unchanged.
- Reviewed `sync/feat/modernize-tray-device-scrollbar` at `a4852dc`: its product scope is limited to the custom tray-device scrollbar in `NotifyIconResources.xaml`, focused four/five-device tests in `PowerTray.Tests/Program.cs`, and two archived task records. `git merge-tree --write-tree` completed without conflict.
- Merged the feature branch with merge commit `4a07f38` (`Merge tray device scrollbar into PowerTray 1.4.3`). The merged Debug solution rebuild completed with 0 warnings and 0 errors. The test program passed both the low-battery icon test and tray-menu scrollbar/theme test before reaching the final directional IPC test, which remains blocked by the formal 1.4.2 process owning the fixed named pipes.
- Pushed merge commit `4a07f3881cc8cddbd44d4ec986d2fdfc761dfa56` to private `sync/main`; local and remote hashes matched.
- Windows VM102 had no tracked changes. After confirming `a4852dc` is contained in merged `main`, a non-forced `git fetch origin main:main` followed by `git switch main` placed its canonical checkout on `main` at `4a07f38`. The pre-existing untracked `NUL` status remained exactly unchanged.
- Ubuntu VM101 fast-forwarded clean `main` from `ae8a56f` to `4a07f38` and remained clean.

## Result

- PowerTray 1.4.3 development code and `feat/modernize-tray-device-scrollbar` are merged on private `sync/main`. The current Windows checkout, Windows VM102, and Ubuntu VM101 all resolve `main` to merge commit `4a07f38`; Ubuntu and the current Windows checkout are clean, while VM102 has no tracked changes and retains only its pre-existing untracked `NUL`.
- Public `origin`, tags, releases, assets, installers, and the formal installed 1.4.2 runtime were not changed.
